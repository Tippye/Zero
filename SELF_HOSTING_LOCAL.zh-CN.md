> 当前 1.0.0 使用说明请见[中文首页](README.zh-CN.md)和[Docker Compose 部署文档](deploy/README.zh-CN.md)。本文保留早期开发阶段记录，其中的功能清单、入口和认证方式不代表当前 1.0.0 版本。

# 本机自托管模式

入口：<http://localhost:3005/>。默认进入「设置 → 连接」（`/settings/connections`）；没有邮箱时显示「尚未配置邮箱」，点击「添加连接」即可配置，无需 Google 账号。

添加弹窗统一提供 Gmail、QQ、163、126、iCloud 和其他 IMAP/SMTP 邮箱。预设服务商只需填写邮箱和授权码，高级设置可覆盖登录用户名、独立 SMTP 凭据及已发送副本选项。自定义邮箱需填写允许列表内的服务器。验证收信与发信成功后，账号显示在同一连接列表；点击「打开邮箱」进入 Zero 统一邮件界面，并选中对应邮箱。IMAP 账号的移除也统一在这里操作，不删除服务器邮件。

## 已启用的行为

- `SELF_HOSTED=true` / `VITE_PUBLIC_SELF_HOSTED=true`：服务器及前端同时启用自托管模式。
- 首次访问由 Better Auth 创建浏览器工作区会话，API 仍验证签名会话，并以服务器会话中的用户 ID 隔离邮箱。未持有会话的请求不能读取邮箱或调用 AI。
- 邮箱连接支持 QQ、163、126、iCloud 及允许列表内的自定义 IMAPS/SMTP 服务；不是新增 POP3 实现。
- Google 为可选连接。在访客工作区选择 Google 时使用账号关联，保留该工作区的 IMAP 所有权，不主动删除访客数据。
- 订阅不再限制自托管功能，不调用 Autumn 创建客户、扣减计费用量或订阅升级。需要单独外部服务的功能仍需其自己的配置。

邮箱授权码仍加密保存在桥接数据卷中。浏览器 Cookie 用于识别工作区：不同浏览器/无痕窗口默认各自独立；清除 Cookie 会失去原访客工作区的访问凭据。不要将当前不要求人工登录的本机服务直接作为公共共享邮箱入口；现有服务仅监听回环地址。

## 统一多邮箱界面

[所有邮箱收件箱](http://localhost:3005/mail/inbox) 使用原来的 Zero 邮件界面，聚合当前工作区的 OAuth 和 IMAP 邮箱。草稿箱与已发送同样可聚合查看；每行显示所属邮箱。侧边栏按邮箱地址增加可折叠分组，分别提供收件箱、草稿箱、已发送、归档、垃圾邮件和回收站。

`/mail/inbox?accountId=邮箱ID` 表示只查看该邮箱，省略 `accountId` 表示所有邮箱。旧 `/imap?accountId=...` 仅作为兼容跳转，不再有独立 UI。切换邮箱不再修改服务器默认连接，多个标签页可各自查看不同邮箱。切换文件夹或邮箱时清空批量选择；邮件 ID 包含所属邮箱，批量操作按邮箱分别执行。

列表按时间合并，分页保留各邮箱尚未显示的邮件 ID，避免遗漏。IMAP 列表行使用邮件头预览，打开邮件才读取正文。单个邮箱读取失败时显示提示，并保留其他邮箱的结果；重新刷新可重试失败邮箱。

新建邮件显示发件邮箱选择。已有草稿、回复使用其原邮箱；IMAP 草稿保存在邮箱服务器中，先追加新版本再移除旧版本，安全替换/删除要求服务端支持 UIDPLUS。发送使用同一草稿，只有 SMTP 确认接受邮件后才清理草稿；无法确认时保留草稿和操作 ID，避免自动重复发送。是否额外保存已发送副本仍使用连接时的设置（QQ 等服务通常自行保存）。

IMAP 暂按单封邮件展示；Gmail 继续使用会话。Gmail 专属的自定义标签、分类、暂停和邮箱代理聊天在相应 OAuth 邮箱中使用；IMAP 保留原编辑器的 AI 写作入口，但不提供定时发送。普通收发与阅读不需要 LLM。

验证脚本：`.local/check-unified-mail.cjs`（实际 tRPC 过程、模拟服务商）、`.local/check-unified-ui.cjs`（浏览器、模拟邮箱）及 IMAP bridge 的协议测试。测试不向真实收件人发送邮件、不清理用户已有草稿。

## 界面语言

支持简体中文（`zh-CN`）和繁體中文（`zh-TW`）。连接页及登录页可通过「语言 / Language」切换，原邮箱界面也可在常规设置中选择。选择会保存到浏览器 Cookie，刷新后仍保留；切换语言不会更换邮箱工作区。

首次访问按浏览器首选语言识别；中文香港、澳门及 `zh-Hant` 使用繁体，`zh-Hans` 使用简体。没有匹配语言时使用英文。翻译文件位于 `apps/mail/messages/zh-CN.json` 和 `apps/mail/messages/zh-TW.json`；修改词条后重新构建前端即可生效。

已验证：两套词条的键名、插值及复数结构一致；全新浏览器自动识别简体/繁体；手动切换与刷新保留；登录页中文；HTML 语言标记随选择更新；切换前后工作区身份不变。

## AI 配置

默认入口：[设置 → LLM 服务商](http://localhost:3005/settings/llm)。不需要在 `.env` 中设置 OpenAI 变量即可添加配置。填写配置名称、`Base URL (OPENAI_BASE_URL)`、API Key 和模型 ID，点击「获取模型列表」可从该 URL 下的 `/models` 接口选择模型；不提供模型列表的服务可手动填写模型 ID。

每个账号/访客工作区可保存最多 20 个配置，同时只有一个激活的配置。保存时可勾选「保存后激活」，也可从列表切换。编辑时 Key 留空表示保留，修改 Base URL 后必须重新填写 Key。删除或停用当前配置后，AI 暂停，直到手动激活其他配置，不会悄悄改用其他服务。

未配置或未激活时，AI 操作会显示提示和「配置 LLM」入口。写作/聊天中的入口在新标签页打开设置，以保留未提交内容；普通收发邮件不依赖 LLM。

服务器原有 `.env` 配置仍可使用，在列表中显示为「服务器配置（.env）」：

```dotenv
OPENAI_BASE_URL=https://your-provider.example/v1
OPENAI_API_KEY=your-key
OPENAI_MODEL=your-model
OPENAI_MINI_MODEL=your-small-model
```

地址别名的优先级为 `OPENAI_BASE_URL`、`OPENAI_URL`、`OPEN_URL`。值为 API 前缀，不包含 `/chat/completions` 或 `/models`。新工作区在 `.env` 有 Key 时默认使用它；设置中激活其他配置后则使用新配置。环境配置为只读选项，服务器 Key 不回传给浏览器。

网页配置的 Key 使用 AES-GCM 加密保存在 PostgreSQL `mail0_user_llm_settings` 表中，以 `BETTER_AUTH_SECRET` 派生加密密钥，并绑定工作区与配置 ID。请同时备份数据库和该 Secret；更换 Secret 后需要重新录入网页配置的 Key。接口只返回「Key 已保存」，Key 不进入持久查询/变更缓存，也不经过原项目的请求正文日志中间件。

普通远程服务使用 HTTPS；本机自托管可填写 `http://localhost:端口/v1` 或 `http://127.0.0.1:端口/v1`。服务地址必须可从后端访问。本地无鉴权的 OpenAI 兼容服务可填写其接受的占位 Key（如 `ollama`）。请求不跟随重定向，模型列表有 15 秒超时，生成请求有 60 秒超时。

IMAP 摘要、翻译、回复、草拟，以及原收件箱的聊天、主题/查询生成、写作风格和文本摘要，均读取工作区激活的配置。后台邮箱任务根据连接所有者读取配置。IMAP AI 仍需用户勾选内容发送同意，AI 不会自行发送邮件。兼容服务需支持相应的 Chat Completions、结构化输出或工具调用能力。

高级设置中的轻量模型留空时使用主模型。Embedding 模型仅用于语义索引，需要匹配的向量存储；本机原先未启用的 Cloudflare 索引/AutoRAG 不会因此自动启用。

模型发现遵循 [OpenAI 模型列表接口](https://developers.openai.com/api/reference/resources/models/methods/list) 的 `GET /models`、`data[].id` 结构。接口存在不表示其所有模型都支持聊天，请选择服务商支持相应功能的模型。

## Gmail 发送与草稿

自动保存会更新当前草稿，发送时等待正在进行的保存完成，并使用同一草稿 ID。修改主题或收件人也会保存；内容未变化时不重复提交。保存失败后，修改保留在编辑器中，显示重试入口，不会在后台持续重试。

发送期间显示「发送中…」，成功后提供「查看已发送」。Gmail 的已发送列表和详情直接读取 Gmail，避免本地同步滞后隐藏刚发出的邮件；列表保留筛选与分页。发送回执中的会话 ID 也会用于后台同步。

如果发送失败或网络中断导致结果无法确认，请先检查「已发送」再决定是否重试。现有历史草稿不会被自动批量清理，避免误删未发送内容。

回归脚本：`node .local/check-draft-save.cjs`、`node .local/check-gmail-receipt.cjs`、`node .local/check-gmail-send.cjs`。浏览器脚本需本机前端运行；所有邮箱接口均使用模拟响应，不发送真实邮件。

## 运行与验证

在 `/home/tippy/Zero` 执行：

```bash
.local/manage.sh status
.local/manage.sh restart
.local/manage.sh stop
```

改变前端配置后：

```bash
python3 .local/sync-env.py
pnpm --filter @zero/mail build
docker compose -p zero-web -f .local/compose.web.yaml up -d --force-recreate
```

用户表增加 `is_anonymous` 字段，本机已通过 `pnpm db:push` 更新。现有 Google 用户及邮箱数据保留。部署到其他数据库时也要应用此 schema 变更。

已验证：全新浏览器无 Google 登录直接进入空邮箱工作区；刷新保留会话；页面无订阅/自动退出请求和运行时错误；两个新访客会话使用不同身份；无会话请求被拒绝；AI 返回服务器配置状态而不返回 Key；访客实际 AI 草拟请求成功；前端生产构建成功。

全仓 TypeScript 检查仍有旧路由类型生成、Microsoft 驱动接口等错误，不能视为全仓类型检查通过。真实邮箱授权码验证及真实收发需在页面添加邮箱后验收，本次没有代填或发送真实邮件。

### 服务端后台同步与缓存

`zero-mail-sync.service` 独立于网页 API 持续运行，关闭浏览器后也会同步。默认每个邮箱缓存最近 90 天、最多 500 封邮件的索引；正文首次打开后按需缓存，每封最多 5 MB、每个邮箱正文总额最多 50 MB。同步周期默认为 120 秒，可在「设置 → 通用 → 同步与本地缓存」修改。附件不预下载，超限只清理本地缓存，不删除邮箱服务商中的任何邮件。

常规收件箱、草稿、已发送等列表和搜索从本地 PostgreSQL 读取。搜索仅覆盖缓存范围；首次打开未缓存正文、发送和修改邮件仍需访问邮箱服务商。页面显示后台同步状态和更新时间；「立即同步」只排队，不阻塞页面。现有稍后提醒文件夹沿用原实现。

Gmail 首次拉取有限的最近邮件头，之后通过 history 增量更新，失效时重建有限窗口。IMAP 以独立只读连接同步有限范围的邮件头；用户操作使用另外的连接。任务限并发，限流后退避，服务重启会恢复任务。缓存数据存放在本机 PostgreSQL 卷中，应按邮件数据保护其访问权限和备份。

服务定义：`.local/zero-mail-sync.service`；详细安装与表结构见 `integrations/mail-sync/README.md`。`MAIL_SYNC_ENABLED=true` 控制 API 是否使用本地索引。`.local/manage.sh` 已包含同步服务的启动、停止和状态检查。

### AI 邮件分类

收件箱顶部默认显示“主要 / 交易 / 更新 / 推广 / 所有”。默认名称随界面语言切换，自定义名称保留原文。
旧默认分类自动升级，已有自定义分类保留。同步状态位于侧边栏 Live Support 上方。

后台同步服务使用“设置 → LLM”中当前激活配置的小模型（未设置小模型时使用主模型），每批最多分类 10 封邮件。
仅将发件人地址与主题发送给配置的 Provider，不发送正文或附件；基于元数据的分类可能不完全准确。
分类结果存储在本地缓存，不修改邮箱原有标签。未分类邮件始终可在“所有”查看。
未配置模型时分类栏提供设置入口；API 暂时失败时后台退避重试，不影响邮件读取。
