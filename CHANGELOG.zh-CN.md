# 更新说明

## 配对认证与 Apple 原生源码 — 2026-09-07

- 自托管登录统一改为配对码／二维码，由已登录设备或服务器终端批准；移除默认用户名密码，支持设备会话查看、撤销及旧工作区迁移。
- 新增 macOS、iOS/iPadOS、独立 watchOS 的 SwiftUI/Xcode 工程，共享配对认证、Keychain 存储与邮件 API；接入邮件阅读、写信、草稿、附件及适用平台的 Handoff、邮件链接和 App Intents。
- 后端新增经过会话验证的原生邮件 API，复用现有工作区权限和邮箱操作。
- 提供 [Mac 迁移说明](native/apple/MIGRATION.zh-CN.md)和 Apple CI 配置。Linux 上已检查共享模块、原生 API 与工程结构；尚未完成 Xcode 编译、真机与签名验收。APNs、Widget、完整离线存储及商店发布仍待后续开发。

认证操作与升级步骤见[配对认证](deploy/AUTHENTICATION.zh-CN.md)。

## 邮件体验修复 / Android 1.0.1 — 2026-09-06

- 移除安卓常驻返回/菜单栏，应用设置移至「设置 → 通用」；登录成功替换历史记录，已有会话访问登录页自动返回邮件。
- 设置新增跨邮箱 AI 分类进度、立即分类、暂停、继续和重新分类。暂停保存到服务器并中止在途请求；重新分类保留人工分类。
- 分类请求支持 JSON 模式、现代/旧版 token 参数兼容与输出截断提示；格式失败减少批量大小重试，界面显示实际重试时间。
- 邮件列表按工作区隔离缓存，失效分页自动从第一页恢复；文件夹同步错误只在相应文件夹警示，保留其他缓存结果；重试按钮触发后台同步，邮件列表轮询状态并自动刷新。
- HTML 和纯文本正文跟随应用主题，修正行内黑字/白底的深色对比，支持邮件自身的深色媒体查询，不反转图片。
- 移除标题下方空标签遗留的竖线；自托管环境停用未配置的客服接口调用。

## Android 1.0.0 开发版 — 2026-09-06

- 新增独立 Java/Gradle 安卓客户端，连接 Zero Compose 服务器，支持原生地址设置、账号会话、返回键和连接错误恢复。
- 接入系统附件选择/保存、冷/热启动邮件链接和 Android 文本分享。
- 新增默认关闭的后台定期邮件通知，隐藏邮件内容；切换服务器清理本机会话和通知状态。
- 提供 Gradle Wrapper、debug APK、正式签名配置、自动化测试及 Android 构建工作流。
- 需要网络；后台检查不等于实时推送。Gmail 原生 OAuth 回传、离线收发及商店发行未包含在此版。

详情见[安卓说明](native/mobile/README.zh-CN.md)及[独立验收记录](native/mobile/VALIDATION.zh-CN.md)。

## 1.0.0 — 2026-09-06

本分支的首个可用 Windows 客户端版本，配合当前 Docker Compose 服务端使用。客户端包内版本、锁文件和安装包版本统一为 `1.0.0`。

### Windows 客户端

- 使用简洁的自定义标题栏，隐藏传统菜单栏；左侧 Zero Mail 下拉菜单集中常用操作。
- 保留窗口拖动、Windows 原生最小化/最大化/关闭按钮，以及浅色、深色外观。
- 支持新建邮件、刷新和缩放快捷键，以及关闭到托盘、重新连接和正常退出。
- 保留系统通知、`mailto:` / `zeromail:` 协议、服务器连接与默认邮件应用设置。

### 邮件界面

- 移除“新建邮件”上方的用户信息与重复的邮箱连接入口。
- 设置按钮与三个点菜单左右排列，收起侧边栏时也可使用。
- 移除菜单外重复的同步状态显示；连接管理保留在设置页面。

### LLM 配置

- 修复 Docker 中将本机模型地址错误地解析为容器自身而导致的连接失败。
- 模型列表、模型测试、邮件 AI 和后台分类使用一致的宿主机映射配置。
- 自托管模式支持本机、私有局域网 IP 和 `host.docker.internal` 的 HTTP API 地址。
- 新增“测试模型”按钮，可测试已保存配置或尚未保存的表单，显示响应耗时。
- 区分网络、超时、认证、权限、限流、配额及响应格式等错误。
- 测试使用简短消息，不读取邮件内容；密钥不会回显到设置表单或错误提示。

### 文档

- 新增[中文首页](README.zh-CN.md)和当前更新说明。
- 更新 Docker 部署、Windows 安装/构建、LLM 配置和验收记录。
- 明确服务端升级与客户端重装的区别，并保留早期文档作为历史参考。

### 升级

1. 保留已有数据卷与加密配置，按[部署说明](deploy/README.zh-CN.md#数据和升级)更新服务端。
2. 完全退出旧版 Windows 客户端，安装 `Zero Mail Setup 1.0.0.exe`；便携用户使用对应 ZIP。
3. 打开客户端，在“服务器与桌面设置”检查版本号，并按 `Ctrl+R` 刷新邮件页面。

本次不自动推送 Git 标签或上传安装包。功能验证和本机构建文件的 SHA-256 见[验收记录](deploy/VALIDATION.zh-CN.md)。

### 已知限制

- 安装程序尚未签名；便携 ZIP 不注册默认邮件处理程序。
- 不支持完整离线收发或 MAPI/COM 集成。
- Compose 运行时不提供可靠的定时发送与延迟撤回，相关入口保持关闭。
- 不将 Windows 单机验收等同于所有邮箱供应商、所有 Windows 环境或移动端的兼容性保证。
