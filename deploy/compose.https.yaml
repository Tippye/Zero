services:
  web:
    ports: ['${HTTPS_BIND:-127.0.0.1}:${HTTPS_PORT:-8443}:443']
    volumes:
      - ./deploy/nginx/https.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/tls:/etc/zero/tls:ro
