{
  "name": "zero-mobile",
  "version": "1.0.1",
  "private": true,
  "description": "Zero Mail Android client for self-hosted servers, with a separate experimental iOS scaffold.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "add:ios": "cap add ios",
    "sync": "npm run prepare:web && cap sync ios",
    "open:ios": "cap open ios",
    "android:debug": "node scripts/android.mjs assembleDebug",
    "android:release": "node scripts/android.mjs release",
    "android:check": "node scripts/android.mjs testDebugUnitTest lintDebug assembleDebug assembleDebugAndroidTest",
    "android:device-test": "node scripts/android.mjs connectedDebugAndroidTest",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
